# Final Route Ownership Matrix

Status 2026-09-15. Authoritative, current-state route/origin ownership table, generated directly
from the executable contract (`apps/web/src/lib/route-ownership.ts`) rather than hand-maintained —
`route-ownership.test.ts`'s exhaustive real-filesystem walk CI-enforces zero `UNKNOWN` for every
route that actually exists. Evidence class: SOURCE INSPECTION + AUTOMATED TEST +
PRODUCTION LIVE HTTP (spot-checked; full detail in the Stage A/B/C evidence files).

| Route family                                                                                                                                                                                                                                               | Owner origin                                | Auth                                                                | Methods                              | CSRF rule                                                                   | Wrong-host behavior                                 | Redirect                      | Indexability                                               | Cache                                  | Analytics                     | Evidence                                                                             |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------ | --------------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------- | ---------------------------------------------------------- | -------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------ |
| Public marketing/content (`/`, `/pricing/`, `/guides/**`, `/crawlers/**`, `/platforms/**`, `/for/**`, `/research/**`, `/observatory/**`)                                                                                                                   | `crawlpact.com`                             | none                                                                | GET/HEAD                             | exempt (safe)                                                               | n/a (public, no app-host equivalent)                | canonical-slash 301 only      | indexable, in sitemap                                      | public cache per `CDN_CACHE_POLICY.md` | GA4/Clarity post-consent only | `PRODUCTION_CUTOVER_EVIDENCE.md` §3                                                  |
| `/status/`, `/robots.txt`, `/sitemap.xml`, `/.well-known/security.txt`                                                                                                                                                                                     | `crawlpact.com`                             | none                                                                | GET/HEAD                             | exempt                                                                      | n/a                                                 | none                          | indexable (status/robots/sitemap); security.txt not a page | public cache                           | GA4/Clarity post-consent only | `PRODUCTION_CUTOVER_EVIDENCE.md` §3                                                  |
| `/audit`, `/audit/:id`, `/pay`, `/shared/:token`, `/feed/:token`                                                                                                                                                                                           | `crawlpact.com` (`PUBLIC_ONLY`)             | none (capability-URL access)                                        | GET/HEAD (+POST `/audit`)            | exempt for GET; `/api/audit` POST is `PUBLIC_ONLY` API, no session to forge | 404 on app host                                     | none                          | `noindex`                                                  | `private, no-store`                    | none                          | `route-ownership.ts` `isPublicOnlyPath`                                              |
| `/api/audit`, `/api/audit/:id`, `/api/audit/:id/report`, `/api/audit/continuation/:id`                                                                                                                                                                     | `crawlpact.com` (`PUBLIC_ONLY`)             | none                                                                | POST/GET                             | exempt (no session)                                                         | 404 on app host                                     | n/a                           | n/a                                                        | `private, no-store`                    | none                          | `route-ownership.ts` API rules                                                       |
| `/api/billing/webhook`                                                                                                                                                                                                                                     | `crawlpact.com` (`SERVER_TO_SERVER_PUBLIC`) | Paddle signature                                                    | POST                                 | n/a (server-to-server, signature-verified)                                  | 404 on app host                                     | n/a                           | n/a                                                        | `private, no-store`                    | none                          | `PRODUCTION_CUTOVER_EVIDENCE.md` §9                                                  |
| `/api/analytics/track`                                                                                                                                                                                                                                     | both hosts (`SHARED_SAME_ORIGIN_SURFACE`)   | none                                                                | POST                                 | self-referential (same-origin only, either host)                            | n/a — the one route intentionally reachable on both | n/a                           | n/a                                                        | `private, no-store`                    | first-party event only        | `route-ownership.ts` API rules                                                       |
| `/sign-in`, `/app`, `/app/**`, `/admin`, `/admin/**` (legacy apex paths)                                                                                                                                                                                   | `app.crawlpact.com` (`APP_ONLY` pages)      | app-side (session or ceremony)                                      | GET/HEAD redirect; other methods 404 | n/a (redirect only)                                                         | on apex: `308` to app host; unsafe methods 404      | **`308` permanent** (Stage B) | app host `noindex`                                         | `private, no-store` (app host)         | none on app host              | `PHASE_4_STAGE_B_EVIDENCE.md`                                                        |
| `/api/auth/**`, `/api/account/**`, `/api/billing/**` (non-webhook), `/api/domains/**`, `/api/groups/**`, `/api/workspace/**`, `/api/notifications/**`, `/api/app/**`, `/api/admin/**`, `/api/agency-branding/**` (except logo GET), `/api/audit/:id/share` | `app.crawlpact.com` (`APP_ONLY`)            | session (most) or WebAuthn ceremony (`/api/auth/register,login/**`) | varies                               | self-referential, app-origin only                                           | 404 on apex                                         | n/a                           | n/a                                                        | `private, no-store`                    | none                          | `PHASE_4_STAGE_C_EVIDENCE.md` (ceremony origin), `PRODUCTION_CUTOVER_EVIDENCE.md` §4 |
| `/api/agency-branding/logo/[...key]`                                                                                                                                                                                                                       | `crawlpact.com` (`PUBLIC_ONLY`)             | none                                                                | GET                                  | exempt                                                                      | 404 on app host                                     | n/a                           | n/a                                                        | asset cache                            | none                          | `PHASE_4_STAGE_A_STATUS.md` (agency-logo fix)                                        |
| `/api/test-only/**`                                                                                                                                                                                                                                        | neither (`INTERNAL_ONLY`)                   | test-harness only                                                   | varies                               | n/a                                                                         | 404 on both real hosts in Production                | n/a                           | n/a                                                        | `private, no-store`                    | none                          | `route-ownership.ts` API rules                                                       |

## Zero UNKNOWN / UNRESOLVED

`route-ownership.test.ts`'s exhaustive real-filesystem walk (`readdirSync`/`statSync` over every
file under `apps/web/src/pages/api/**`) asserts `classifyApiOwnership` never returns `"UNKNOWN"`
for a route that actually exists — re-run this session, 31/31 passed. No real Production route is
`UNKNOWN` or left ambiguous.

## Redirect status finality

`LEGACY_REDIRECT_STATUS` (`legacy-redirect.ts`) is `308` — Stage B, permanent, live in Production
since `PHASE_4_STAGE_B_EVIDENCE.md`'s deployment. Not `307`.

## WebAuthn ceremony origin (not a route-ownership dimension, recorded here for completeness)

`webauthnCeremonyOrigins()` (`webauthn.ts`) restricts ceremony begin/finish to the app origin once
one is configured — Stage C, live since `PHASE_4_STAGE_C_EVIDENCE.md`. `WEBAUTHN_RP_ID` remains
`crawlpact.com`, unchanged.
