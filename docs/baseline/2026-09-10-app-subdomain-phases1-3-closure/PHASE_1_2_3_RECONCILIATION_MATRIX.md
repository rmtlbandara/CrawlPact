# Phase 1–3 Reconciliation Matrix

Status as of 2026-09-10. One row per major acceptance area from the Phase 1/2/3 directives, ADR-0010,
RISK-036, and the Phase 1–3 closure directive. Historical Phase 1/2/3 evidence is unedited; where a
row corrects an earlier report's claim, the correction lives in `STARTING_STATE.md` and the
now-updated "current state" docs (`docs/status/CURRENT_STATE.md`,
`docs/deployment/CLOUDFLARE_ENVIRONMENT_MATRIX.md`, `CLOUDFLARE_CONFIGURATION.md`,
`docs/risks/ACTIVE_RISKS.md`), not by rewriting the Phase-dated folders.

Statuses used: `PASS`, `PASS_WITH_FRESH_EVIDENCE`, `SUPERSEDED`, `FIX_REQUIRED`, `EXTERNAL_PENDING`,
`HUMAN_VALIDATION_REQUIRED`, `NOT_APPLICABLE`.

| Area                                                                                      | Status                                                  | Notes / blocks Phase 4?                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cloudflare Custom Domain (`app.crawlpact.com`)                                            | **PASS_WITH_FRESH_EVIDENCE**                            | Live-reverified 2026-09-10 via direct API read (domain id, cert, enabled). Does not block.                                                                                                                                                                                                                                                                                          |
| Preview Custom Domain (`preview.crawlpact.com`)                                           | **PASS_WITH_FRESH_EVIDENCE**                            | Also live-reverified; several "current state" docs still described it as pending — corrected this pass. Does not block.                                                                                                                                                                                                                                                             |
| Static Assets literal-alias boundary (prerendered pages)                                  | **PASS**                                                | Fixed and tested in Phase 3 (#172); unchanged, still 27/27 host-boundary tests green. Does not block.                                                                                                                                                                                                                                                                               |
| Static Assets synthetic-alias boundary (`/for/*`, `/research/*`)                          | **FIX_REQUIRED → fixed this pass**                      | Real latent bug (malformed `.../index.html/` redirect target), never exploitable via a real Cloudflare request but a real reachable URL defect. Fixed in `route-registry.ts`'s `resolveCanonicalRedirectTarget`, covered by new unit + host-boundary tests, all green. Not yet deployed to Production — pending PR/Preview/Production sequence.                                     |
| `PUBLIC_APP_URL` schema (required for deployed envs)                                      | **FIX_REQUIRED → fixed this pass**                      | Schema tightened (`packages/config/src/env.ts`): required + HTTPS + distinct-from-`PUBLIC_SITE_URL` for `preview`/`production`, still optional for `local`. 23/23 `env.test.ts` tests green; `pnpm env:validate:{preview,production}` both still pass unmodified against real committed config. Not yet deployed (this is a build/CI-time gate, not a runtime check — see below).   |
| Preview WebAuthn/app-origin hostname design                                               | **FIX_REQUIRED → fixed this pass**                      | `app-preview.crawlpact.com` was a sibling of, not a subdomain of, `preview.crawlpact.com` — would have silently failed every WebAuthn ceremony the moment it was ever attached. Corrected to `app.preview.crawlpact.com` in `wrangler.jsonc`/`scripts/build.sh`/tests. No Custom Domain attached for it (unchanged) — this only fixes the candidate value before anyone acts on it. |
| Production `workers.dev` exposure                                                         | **HUMAN_VALIDATION_REQUIRED (held for owner sign-off)** | Confirmed still enabled. Disabling it is a real, account-level Cloudflare change (`"workers_dev": false` in `wrangler.jsonc` plus an account-level API call) — held for explicit go-ahead rather than made from this reconciliation pass. Does not block Phase 4 on its own (it's redundant exposure, not a broken boundary), but should be resolved before Phase 4.                |
| Cloudflare BIC exception (Paddle remediation)                                             | **PASS_WITH_FRESH_EVIDENCE**                            | Reverified: exactly 1 rule, unchanged scope/expression, zone-wide `security_level`/`browser_check` unaffected. Not modified this pass — directive Section 18 says wait for Paddle approval before reassessing.                                                                                                                                                                      |
| Managed WAF / DDoS / rate limiting                                                        | **NOT_APPLICABLE (documented, not a gap to close)**     | Zone is Free Website plan — Managed WAF isn't available to enable at all, not merely unconfigured. Pre-existing, unrelated to this migration. Documented in `CLOUDFLARE_CONFIGURATION.md` this pass so it's no longer an unstated gap. Does not block Phase 4 (nothing changed here, nothing to fix).                                                                               |
| Paddle checkout-domain status (`app.crawlpact.com`)                                       | **EXTERNAL_PENDING**                                    | Owner-supplied evidence: Action required (pre-resubmission). Owner has agreed to resubmit the current already-remediated state. No Paddle MCP/API route available to this session (MCP down, no usable API key). **Hard Phase 4 gate — blocks.**                                                                                                                                    |
| Google Authorized JavaScript Origin (`app.crawlpact.com`)                                 | **EXTERNAL_PENDING — cannot verify**                    | No Google Cloud/OAuth tooling connected, confirmed by direct probe. Phase 3's own finding, still true. **Blocks Phase 4** until independently confirmed (by the owner, or by a future connected tool).                                                                                                                                                                              |
| Real Google Sign-In end-to-end on `app.crawlpact.com`                                     | **HUMAN_VALIDATION_REQUIRED**                           | Not performed — requires either a real account + browser interaction or connected tooling, neither available/authorized this pass. Blocks Phase 4.                                                                                                                                                                                                                                  |
| New passkey registration + auth on `app.crawlpact.com`                                    | **HUMAN_VALIDATION_REQUIRED**                           | Not performed this pass — real production auth-state mutation, held for explicit go-ahead. Existing pre-migration passkey continuity already PASSed (Phase 3). Blocks Phase 4's stated acceptance criteria (RISK-036).                                                                                                                                                              |
| Live cookie-isolation proof (host-only, no `Domain`)                                      | **HUMAN_VALIDATION_REQUIRED**                           | Not performed — requires a real authenticated browser session. Source-level guarantee unchanged and re-verified by reading `session.ts` (host-only, no `Domain` attribute, untouched by any change this pass). Blocks Phase 4's stated acceptance criteria.                                                                                                                         |
| Live authenticated sibling-origin CSRF proof                                              | **HUMAN_VALIDATION_REQUIRED**                           | Server-level integration suite (4 sibling-origin tests) already passes; live proof against a real session not performed. Blocks Phase 4's stated acceptance criteria.                                                                                                                                                                                                               |
| WebAuthn RP ID / origin-pinning regression                                                | **PASS_WITH_FRESH_EVIDENCE**                            | `WEBAUTHN_RP_ID=crawlpact.com` reconfirmed live and unchanged; origin-pinning code unmodified this pass; full unit+security suites green (677/677, 45/45).                                                                                                                                                                                                                          |
| CSRF regression                                                                           | **PASS_WITH_FRESH_EVIDENCE**                            | `test:security` (includes `csrf.integration.test.ts`) green, 45/45; unmodified by this pass's fixes.                                                                                                                                                                                                                                                                                |
| GSC / GA4 / CrUX verification                                                             | **EXTERNAL_PENDING — cannot verify**                    | No connected tooling, confirmed by probe. Same conclusion as Phase 3. Does not block Phase 4 on its own (SEO hygiene, not a security/auth gate) but should be resolved before customer traffic moves.                                                                                                                                                                               |
| `/pay` and Paddle webhook apex non-regression                                             | **NOT_RE-VERIFIED THIS PASS**                           | No code touched either endpoint this pass; last verified in Phase 2/3. Recommend a fresh live check before Phase 4, not urgent for this closure pass.                                                                                                                                                                                                                               |
| Full regression suite (format/lint/typecheck/unit/integration/security/db:validate/build) | **PASS**                                                | All green this pass, including the new tests for the two fixes above: format ✓, lint ✓ (0 warnings), typecheck ✓ (0 errors), unit 677/677 (52 files), integration 394/394 (56 files), security 45/45 (8 files), db:validate ✓ (56 tables), build ✓.                                                                                                                                 |
| `WEBAUTHN_RP_ORIGIN` legacy field                                                         | **NOT_APPLICABLE for this pass**                        | Still bound, still unread for verification (Phase 2 finding, unchanged). Directive Section 46 asks for a retain-vs-remove decision — deferred to Phase 4 rollback-window planning, since removing it now has no benefit and the field is harmless while retained.                                                                                                                   |
| `/dev/components` runtime protection (Phase 1 secondary finding)                          | **NOT_RE-VERIFIED THIS PASS**                           | Logged in Phase 1, not re-checked this pass (out of this pass's scope — not app-subdomain-specific). Should be tracked to closure independently, not silently dropped.                                                                                                                                                                                                              |

## Summary

Every item this session's tooling could safely act on (Cloudflare/git read-only verification, the
two real code defects found, doc reconciliation) is closed or fixed. What remains open is exactly
the set the closure directive itself predicted would need either external tooling this account
doesn't have connected (Google, GSC/GA4/CrUX, Paddle API) or a human/production-state decision
(Paddle resubmission click, `workers.dev` disablement, live authenticated passkey/cookie/CSRF
proof, live Google Sign-In). None of these were skipped, guessed at, or marked PASS without
evidence — see `PHASE_4_READINESS_REPORT.md` (to follow this reconciliation pass) for the
strict verdict.
