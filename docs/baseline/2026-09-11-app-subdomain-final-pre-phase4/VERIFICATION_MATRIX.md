# Final Pre-Phase-4 — Verification Matrix

Status 2026-09-11. This matrix covers only the items newly reported by the owner in this pass's
own instructions (Sections 3.2–3.9) that are not already tracked in
`docs/baseline/2026-09-10-app-subdomain-phases1-3-closure/PHASE_1_2_3_RECONCILIATION_MATRIX.md`
(passkey and Google Sign-In are already there, updated in PR #175, and not repeated here). See
that file for the full Phase 1–3 item list.

Evidence classes: OWNER-OBSERVED, TECHNICAL CORROBORATION, AUTOMATED TEST, LIVE HTTP, READ-ONLY D1,
SOURCE INSPECTION, UNVERIFIED / MANUAL REQUIRED.

| Item                                                                                                                                             | Status                                                                                                              | Evidence                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Live app-host cookie-isolation                                                                                                                   | OWNER-OBSERVED; SOURCE INSPECTION corroborates the mechanism                                                        | Owner reports observing host-only `crawlpact_session` on `app.crawlpact.com`, absent on `crawlpact.com`. `session.ts`'s cookie-issuance code (host-only, no `Domain` attribute) is unchanged since Phase 2 and matches the reported observation. No D1/browser-automation proxy exists to independently re-observe the live cookie this pass.                                          |
| Authenticated sibling-origin CSRF                                                                                                                | OWNER-OBSERVED; AUTOMATED TEST corroborates the mechanism                                                           | Owner reports the exact same-origin/sibling/external-origin pattern the automated suite (`csrf.integration.test.ts`, re-run green this pass, 45/45 security suite) already asserts. The invariant (arrival origin must equal the validated request's own origin, not a broad allowlist) is unchanged in `same-origin.ts` since Phase 2.                                                |
| Live Paddle checkout open/cancel                                                                                                                 | OWNER-OBSERVED; Paddle checkout-domain approval independently re-confirmed via API                                  | `chedom_01m24mn1cqys7mcn80rgt6t4t2` (`app.crawlpact.com`) still `approved`, unchanged timestamp — consistent with a checkout being able to open without a "domain not approved" error. No D1 trace expected or found for a cancelled checkout (Paddle-side state, never reaches CrawlPact's own DB unless a webhook fires, and a cancelled/never-completed checkout doesn't fire one). |
| Recovery authentication                                                                                                                          | OWNER-OBSERVED completion; **HIGH-PRIORITY finding on the exposure follow-up** — see `OWNER_ACTION_QUEUE.md` item 1 | READ-ONLY D1 confirms a real recovery-code batch (created `2026-09-11T02:14:27Z`, 1/10 used) exists for the account most consistent with this test, with **no regeneration evidence** — 9 codes remain live right now.                                                                                                                                                                 |
| Authenticated customer app smoke (`/app`, `/app/workspace`, `/app/domains`, `/app/groups`, `/app/notifications`, `/app/billing`, `/app/account`) | OWNER-OBSERVED                                                                                                      | No new automated coverage added this pass (routes and their tests are unchanged by PR #173–#175). `/app/account`'s reported `Cache-Control: private, no-store` matches this repo's established private-response convention, unchanged.                                                                                                                                                 |
| Ordinary session → admin denial                                                                                                                  | OWNER-OBSERVED, consistent with existing automated coverage                                                         | `admin-security.integration.test.ts` (part of the 45/45 security suite, re-run green) already asserts non-admin sessions get denied on admin routes/APIs.                                                                                                                                                                                                                              |
| Authorized admin read-only smoke (`/admin`, `/admin/health`, `/admin/domains`, `/admin/analytics`, `/admin/audit-logs`)                          | OWNER-OBSERVED; SOURCE INSPECTION confirms all 4 named routes genuinely exist                                       | `ls apps/web/src/pages/admin/` confirms `health`, `domains`, `analytics`, `audit-logs` are all real, existing directories — the owner's route list was not fabricated or mismatched against the actual codebase.                                                                                                                                                                       |
| Public-audit continuation                                                                                                                        | OWNER-OBSERVED only — **no technical corroboration available**                                                      | `audit_continuations` has 0 rows account-wide; source confirms the daily retention cron deletes consumed/expired rows, which already ran once in this window — an empty table is consistent with either a successful consume-then-sweep or the flow never running. Genuinely inconclusive, not a negative signal.                                                                      |
| Pricing → auth → billing continuation                                                                                                            | OWNER-OBSERVED only                                                                                                 | No D1 table captures in-flight `plan`/`interval` query-parameter state (by design — it's a URL-carried value, never persisted until a real checkout). Existing automated coverage (plan/interval allowlist, `isSafeRelativeRedirect`, server-side price re-resolution) is unchanged and re-confirmed green this pass.                                                                  |

## What this matrix does not do

It does not upgrade any OWNER-OBSERVED item to independently proven merely because the owner's
description matches the expected/designed behavior — matching expectations is circumstantial
support, not proof, and is labeled as such throughout.
